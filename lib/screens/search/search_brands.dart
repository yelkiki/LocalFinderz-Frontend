import 'package:flutter/material.dart';

class SearchBrands extends StatelessWidget {
  const SearchBrands({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}